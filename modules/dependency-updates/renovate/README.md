# Renovate policy

Copy `files/.github/renovate.json5`, then set the timezone, schedule, labels,
and exclusions. It groups compatible updates, maintains lockfiles, handles
vulnerability alerts, and discovers GitHub Action/container-image references.
Do not install it with Dependabot.
