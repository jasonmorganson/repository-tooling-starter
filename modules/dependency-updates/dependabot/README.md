# Dependabot policy

Copy `files/.github/dependabot.yml`, then list only the ecosystems and
workspace directories the repository owns. This is the minimal GitHub-native
option. Do not install it with the Renovate module.
