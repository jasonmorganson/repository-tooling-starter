# hk JavaScript profile

Copy `files/.config/hk/javascript.template.pkl`, import it from the root hk
config, then replace every `__...__` placeholder. Keep all execution routed
through `mise run` or `mise exec`; the fragment supplies step shapes, not a
formatter, linter, test directory, or path policy.
